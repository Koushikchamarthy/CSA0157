#include <stdio.h>

int main() {
  int i = 2;
  int n;

  printf("Enter the maximum number in the series: ");
  scanf("%d", &n);

  while (i <= n) {
    printf("%d\n", i);
    i += 2;
  }

  return 0;
}
